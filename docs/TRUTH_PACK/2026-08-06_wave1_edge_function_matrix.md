# Wave 1 Edge Function Matrix — 2026-08-06

| Function | Group | Primary caller files | Expected auth model | Secret / key expectation | CORS needed? | Request validation points | Observed status | Notes |
|---|---|---|---|---|---|---|---|---|
| `shop-purchase` | browser/session-bound | `frontend/src/pages/ShopPage.tsx` | signed-in user JWT | scoped admin key only on server | yes | `item_id`, method, auth header | `pass` | `supabase.functions.invoke()` caller is live in `ShopPage.tsx`; function handles `OPTIONS` and `POST`, verifies a bearer JWT with `auth.getUser(token)`, validates `item_id`, and resolves scoped `shop_purchase` admin credentials per request. |
| `discord-link` | browser/session-bound | `frontend/src/pages/DiscordCallback.tsx` | signed-in user JWT + OAuth state | scoped admin key only on server | yes | `code`, `redirect_uri`, method, auth header | `fail` | Browser caller sends JWT + `code` + `redirect_uri`, and the function handles CORS plus scoped `discord_link` admin key use, but server-side validation stops at `code` and `redirect_uri`. OAuth `state` is only checked in the browser, and `ALLOWED_ORIGINS` omits the current `https://hypervibe.online` live origin. |
| `get-pet-balance` | browser/session-bound | no active browser caller found in `frontend/src` or `api` | signed-in user JWT if exposed | scoped admin key only on server | yes if browser-exposed | method, auth header | `needs-doc` | No current browser callsite was found. If re-exposed, the function already handles browser CORS and bearer auth correctly through anon+RLS, but it never narrows the method surface beyond `OPTIONS` and does not need a scoped admin key because it only reads the caller's own `users` row. |
| `mint-pet-auth` | browser/session-bound | `frontend/src/hooks/useMintPet.ts` | signed-in user JWT | scoped admin key only on server | yes | wallet, contract, chain, CID, method | `pass` | `useMintPet.ts` calls it with a bearer token; the function handles `OPTIONS` and `POST`, enforces JWT auth, validates wallet/CID plus the client contract+chain handshake before spending tokens, and resolves scoped `mint_pet_auth` admin credentials. |
| `mint-pet-confirm` | browser/session-bound | `frontend/src/hooks/useMintPet.ts` | signed-in user JWT | scoped admin key only on server | yes | tx hash, pet metadata, method | `pass` | `useMintPet.ts` calls it with a bearer token after the wallet-signed tx confirms; the function handles `OPTIONS` and `POST`, enforces JWT auth, validates the full mint payload, verifies the on-chain receipt and matching `PetMinted` event, and resolves scoped `mint_pet_confirm` admin credentials. |
| `pet-mentor-chat` | browser/session-bound | `frontend/src/components/pets/PetMentorBubble.tsx` | signed-in user JWT | scoped admin key only on server | yes | message body, method, auth header | `pass` | `PetMentorBubble.tsx` invokes it through `supabase.functions.invoke()`. The function handles browser preflight, requires bearer auth, validates `message` length and history shape, and stays on anon+RLS for caller-scoped reads instead of reaching for an admin key. |
| `course-profile` | service / integration | no active browser caller found in `frontend/src` or `api`; external service only | explicit service-to-service auth only | scoped admin key only on server | no browser CORS contract | caller identity, shared secret/header | `needs-doc` | The browser surface is clean: no in-repo browser caller, no CORS block, shared-secret auth via `X-Sync-Secret`, and scoped `course_profile` admin key use are present. The remaining gap is contract clarity: the intended GET-only service path lives in comments/docs, but the code does not return 405 for other verbs. |
| `generate-v2-config` | service / integration | no active browser caller found in `frontend/src` or `api` | explicit service auth | scoped admin key only on server | no browser CORS contract | method, caller secret, inputs | `pass` | `POST`-only service contract now enforced: browser CORS removed, bearer-JWT path removed, inbound `X-Sync-Secret` required, and server-authoritative Discord + qualifying `agent_access` checks preserved before the outbound V2.4 bridge call. |
| `stripe-webhook` | webhook | no active browser caller found in `frontend/src` or `api`; Stripe webhook only | webhook secret only | scoped admin key only on server | no | signature header, method | `pass` | There is no browser caller. The function resolves scoped `stripe_webhook` admin credentials per request and verifies the Stripe signature with `STRIPE_WEBHOOK_SECRET` before doing any work; unsigned requests fail closed even though the code does not emit an explicit early 405 for non-POST methods. |
| `sync-tokens-to-v24` | webhook / integration | no active browser caller found in `frontend/src` or `api`; Supabase DB webhook/backend only | webhook secret or explicit service auth | scoped admin key only on server | no browser CORS contract | secret header, method, payload shape | `pass` | There is no browser caller. The function is POST-only, gates the no-JWT webhook path with `X-Webhook-Secret`, validates payload type/table/positive amount before forwarding, uses `COURSE_SYNC_SECRET` outbound, and only falls back to scoped `sync_tokens_to_v24` admin credentials to resolve `discord_id` when needed. |
