# UI Kit — Empty & Error States

10 patterns: 3 empty, 3 error, 3 warn/transient, 1 celebrate. Every copy block respects the bible: errors are nudges, empties are invitations, never blame the user.

Drop-in pattern: `<div class="state" data-kind="empty|warn|error|offline|celebrate" data-tag="LABEL">`.
