discord.py==2.3.2
# 2.4.0 predates the sb_secret_*/sb_publishable_* key format -- its client-side
# key validation rejects anything that isn't shaped like a legacy JWT.
supabase==2.31.0
python-dotenv==1.0.1
