# Hyper Vibe Course API (apps/api)

## What runs

- Runtime entrypoint: `src/index.js`
- Start command: `npm start`

## What is archived

The folder `src/_archive_ts_prisma_api/` contains an older, unfinished TS/Prisma scaffold that is **not** wired into the runtime entrypoint.

